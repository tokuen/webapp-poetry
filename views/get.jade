block content
html.html_class(onclick="location.reload();")
  head
    mata(name="referrer" content="no-referrer")
  body(onclick='javascript:location.reload()')
      //- .body_class(style="background:url('#{data.long_photo}') no-repeat;background-size:cover;background-attachment: fixed;background-repeat:no-repeat;")
      //- 最大dev
      dev
        //- 背景图片
        img(src='#{data.long_photo}' referrerpolicy="no-referrer" style="width: 100%;height: 100%;object-fit:cover;z-index:-1;")

        //- 上边搜索框dev
        dev
        
        br
        //- 中间内容框（诗歌内容）
        dev(style="position:relative;top:-1500px;")
          p
           font(face="隶书" size="7" style="color:black;")  &nbsp;&nbsp;!{data.content}
        br
        //- 中间右下出自框（诗歌来自和作者）
        dev(align="right" style="position:relative;top:-1000px;")
          p
            //- white
            font(face="隶书" size="7" style="color:black;" ) --出自&nbsp;#{data.content_from}&nbsp;&nbsp;&nbsp;&nbsp;#{data.author}

        dev
        //- 右边详情框（用户头像，喜欢数量，评论数量，转发）

        dev
        //- 下边切换框（首页，收藏，喜欢，历史，上传，用户）

    
